# Core dependencies for the firm-microsim synthetic data + figures pipeline.
# The canonical dependency metadata lives in pyproject.toml; keep this file as
# a lightweight compatibility path for users who still install requirements.
torch>=2.0           # weight-calibration optimizer (Adam) in firm_microsim.calibration
pandas>=1.5          # tabular IO for processed inputs and synthetic output
numpy>=1.23          # numerical core
matplotlib>=3.6      # figures (figures/*.py)
scipy>=1.10          # structural notch and dynamic indifference solves
odfpy>=1.4           # read HMRC .ods source tables (data extraction)
openpyxl>=3.0        # read ONS .xlsx source workbooks (data extraction)
